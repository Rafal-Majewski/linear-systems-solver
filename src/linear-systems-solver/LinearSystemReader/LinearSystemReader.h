#include <LinearSystem/LinearSystem.h>


template <typename T>
class LinearSystemReader {
	private:

	public:
	LinearSystem<T> read();
};

template <typename T>
LinearSystem<T> LinearSystemReader<T>::read() {
}
